ASM = ..\..\ORGASM.EXE
SRC = MAIN.ASM
OUT = TASM.BIN
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT)

$(OUT): $(SRC)
	$(ASM) $(SRC) /N /L=TASM.ERR

clean:
	-$(DELTOOL) $(OUT)
	-$(DELTOOL) TOUT.BIN
	-$(DELTOOL) TASM.ERR
