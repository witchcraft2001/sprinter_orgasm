ASM = ..\..\ORGASM.EXE
SRC = MAIN.ASM
OUT = MIXED.EXE
ROOT = ROOT.ASM
INCA = DIRA\A.ASM
INCB = DIRB\B.ASM
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT)

$(OUT): $(SRC) $(ROOT) $(INCA) $(INCB)
	$(ASM) $(SRC) $(OUT) /E /L=MIXED.ERR

clean:
	-$(DELTOOL) $(OUT)
	-$(DELTOOL) MIXED.ERR
