ASM = ..\..\ORGASM.EXE
SRC = MAIN.ASM
OUT1 = CODE.BIN
OUT2 = COPY.BIN
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT1) $(OUT2)

$(OUT1): $(SRC)
	$(ASM) $(SRC) /N /L=SAVE.ERR

$(OUT2): $(SRC)
	$(ASM) $(SRC) /N /L=SAVE.ERR

clean:
	-$(DELTOOL) $(OUT1)
	-$(DELTOOL) $(OUT2)
	-$(DELTOOL) SAVE.ERR
