ASM = ..\..\ORGASM.EXE
SRC = ERRORS.ASM
OUT = ERRORS.EXE
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT)

$(OUT): $(SRC)
	$(ASM) $(SRC) $(OUT) /E /L=ERRORS.ERR

clean:
	-$(DELTOOL) $(OUT)
	-$(DELTOOL) ERRORS.ERR
