ASM = ..\..\ORGASM.EXE
SRC = HELLO.ASM
OUT = HELLO.EXE
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT)

$(OUT): $(SRC)
	$(ASM) $(SRC) $(OUT) /E /L=HELLO.ERR

clean:
	-$(DELTOOL) $(OUT)
	-$(DELTOOL) HELLO.ERR
