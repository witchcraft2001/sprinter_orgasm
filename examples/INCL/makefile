ASM = ..\..\ORGASM.EXE
SRC = MAIN.ASM
OUT = INCL.EXE
INC = INC\TEXT.ASM
DELTOOL = MKDEL.EXE

.PHONY: all clean

all: $(OUT)

$(OUT): $(SRC) $(INC)
	$(ASM) $(SRC) $(OUT) /E /L=INCL.ERR

clean:
	-$(DELTOOL) $(OUT)
	-$(DELTOOL) INCL.ERR
